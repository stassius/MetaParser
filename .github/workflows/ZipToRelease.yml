name: Create Archive
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Upload Release
      uses: ncipollo/release-action@v1.12.0
      with:
        artifacts: "MetaParser.zip"
        token: ${{ secrets.GITHUB_TOKEN }}
